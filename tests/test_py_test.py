#test_py_test.py
import py

def test_compare_fi_and_fo(setup):
    """
    Parse a note file, send the parsed data to the save method and compare the
    file that was read to what was saved to a temporary directory.
    """
